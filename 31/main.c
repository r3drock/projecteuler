/* How many different ways can £2 be made using any number of coins? */

#include <stdio.h>

#define numcoins 8

const int amount = 200;
const int coins[numcoins] = {200, 100, 50, 20, 10, 5, 2, 1};

int main()
{
	int count = 0;

	printf("count: %d\n", count);
	return 0;
}

